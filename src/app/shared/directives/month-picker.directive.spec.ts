import { MonthPickerDirective } from './month-picker.directive';

describe('MonthPickerDirective', () => {
  it('should create an instance', () => {
    const directive = new MonthPickerDirective();
    expect(directive).toBeTruthy();
  });
});
