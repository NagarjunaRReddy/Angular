import { GenericDateDirective } from './generic-date.directive';

describe('GenericDateDirective', () => {
  it('should create an instance', () => {
    const directive = new GenericDateDirective();
    expect(directive).toBeTruthy();
  });
});
